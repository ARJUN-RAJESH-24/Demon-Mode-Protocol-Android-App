// src/styles/commonStyles.js
import { StyleSheet } from 'react-native';

// These styles are already defined and exported as 'modalStyles' from 'src/components/SharedModals.js'.
// To avoid duplication and maintain a single source of truth, this file will be empty for now.
// If you have truly general-purpose styles that are not specific to modals,
// you can define and export them here.

const commonStyles = StyleSheet.create({
  // Add truly common, non-modal-specific styles here if needed in the future.
});

export default commonStyles;